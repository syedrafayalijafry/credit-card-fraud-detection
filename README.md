# Credit Card Fraud Detection

A machine learning project using the IEEE-CIS Fraud Detection dataset to detect fraudulent transactions. The project includes data preprocessing, feature engineering, model comparison, a Flask backend, and an HTML/CSS/JavaScript frontend for deployment.

## Dataset

This project uses the IEEE-CIS Fraud Detection dataset.